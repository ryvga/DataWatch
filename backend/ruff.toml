line-length = 120

[lint]
select = ["E4", "E7", "E9", "F"]
ignore = [
  # SQLAlchemy relationship targets are intentionally resolved from the model registry.
  "F821",
]

[lint.per-file-ignores]
"tests/conftest.py" = ["E402"]
